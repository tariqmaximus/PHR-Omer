import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PhrMainComponent } from './phr-main.component';

describe('PhrMainComponent', () => {
  let component: PhrMainComponent;
  let fixture: ComponentFixture<PhrMainComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PhrMainComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PhrMainComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
