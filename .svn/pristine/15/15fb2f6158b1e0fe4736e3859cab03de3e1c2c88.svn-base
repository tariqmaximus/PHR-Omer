import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LabResultsAttachmentsComponent } from './lab-results-attachments.component';

describe('LabResultsAttachmentsComponent', () => {
  let component: LabResultsAttachmentsComponent;
  let fixture: ComponentFixture<LabResultsAttachmentsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LabResultsAttachmentsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LabResultsAttachmentsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
