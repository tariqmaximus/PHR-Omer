import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PhrMessageInboxComponent } from './phr-message-inbox.component';

describe('PhrMessageInboxComponent', () => {
  let component: PhrMessageInboxComponent;
  let fixture: ComponentFixture<PhrMessageInboxComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PhrMessageInboxComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PhrMessageInboxComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
